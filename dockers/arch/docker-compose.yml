version: '3.8'

services:
  arch:
    build:
      context: ../../
      dockerfile: dockers/arch/Dockerfile
    image: test_arch
    privileged: true
    network_mode: "host"
    volumes:
      - ../../app:/app
      - ../../dockers/docker.py:/app/docker.py
    cap_add:
      - NET_ADMIN
    container_name: arch
    environment:
      mode: ""
      start: ""
    command: ["python3", "docker.py", "${MODE}", "${START}"]