version: '3.8'

services:
  debian:
    build:
      context: ..\..\
      dockerfile: dockers\debian\Dockerfile
    image: test_debian
    privileged: true
    network_mode: "host"
    cap_add:
      - NET_ADMIN
    ports:
      - "0.0.0.0:6666:5555"
    container_name: debian
    environment:
      branche: ""
      mode: ""
      start: ""
    #python3 docker.py <ветка> <режим> <файл который нужно запускать>
    command: ["python3", "docker.py", "${BRANCHE}", "${MODE}", "${START}"]