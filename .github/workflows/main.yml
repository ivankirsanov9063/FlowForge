on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  ubuntu-client:
    runs-on: [self-hosted, test-client]
    env:
      MODE: client
      START: ClientCLI
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Ubuntu Client
        run: |
          echo "MODE=${MODE}"
          echo "START=${START}"
          
          # Запуск контейнера
          docker-compose -f ./dockers/ubuntu/docker-compose.yml up
          
          docker rm -f ubuntu

  debian-client:
    runs-on: [self-hosted, test-client]
    env:
      MODE: client
      START: ClientCLI
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Debian Client
        run: |
          echo "MODE=${MODE}"
          echo "START=${START}"
          
          docker-compose -f ./dockers/debian/docker-compose.yml up
          
          docker rm -f debian

  fedora-client:
    runs-on: [self-hosted, test-client]
    env:
      MODE: client
      START: ClientCLI
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Fedora Client
        run: |
          echo "MODE=${MODE}"
          echo "START=${START}"
          
          docker-compose -f ./dockers/fedora/docker-compose.yml up
          
          docker rm -f fedora

  arch-client:
    runs-on: [self-hosted, test-client]
    env:
      MODE: client
      START: ClientCLI
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Arch Client
        run: |
          echo "MODE=${MODE}"
          echo "START=${START}"
          
          docker-compose -f ./dockers/arch/docker-compose.yml up
          
          docker rm -f arch